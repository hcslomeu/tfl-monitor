# PROGRESS

Current state of work packages. Update the status column as WPs land.

| Phase | WP | Title | Track | Status | Notes |
|---|---|---|---|---|---|
| 0 | TM-000 | Contracts and scaffold | — | ✅ 2026-04-22 | Scaffold, contracts, tier-1/tier-2 schemas, fixtures fetched |
| 1 | TM-A1 | Docker Compose + Postgres + Redpanda + Airflow | A-infra | ✅ 2026-04-23 | Round 3 follow-up applied: pgcrypto + DB-side defaults on raw tables; integration smoke test gated on `DATABASE_URL` |
| 1 | TM-B1 | Async TfL client + fixtures | B-ingestion | ✅ 2026-04-26 | Async `httpx` client with hand-rolled retry (429/5xx/timeout), tier-1 → tier-2 normalisation (line-status, arrivals, disruptions with synthetic SHA-256 IDs), `app_key` redacted in Logfire spans, fixtures committed under `tests/fixtures/tfl/`. |
| 2 | TM-C1 | dbt scaffold | C-dbt | ✅ 2026-04-23 | Acceptance criteria absorbed by TM-000: `dbt/sources/tfl.yml` mirrors `contracts/dbt_sources.yml` (CI-enforced), `dbt-parse` green, `.gitkeep`s pin empty model dirs. |
| 2 | TM-D1 | FastAPI skeleton + Logfire wiring | D-api-agent | ✅ 2026-04-26 | Locked 501 stubs with parametrised tests, added bidirectional OpenAPI drift test, extended CORS allow-list with `https://tfl-monitor.vercel.app` (G1 + G2 + G4 from research). |
| 3 | TM-B2 | `line-status` producer | B-ingestion | ✅ 2026-04-27 | Async daemon (`LineStatusProducer.run_forever`), 30s fixed-rate cadence, `KafkaEventProducer` wrapper around `AIOKafkaProducer` (idempotent, `acks="all"`, partition key=`line_id`), `event_type="line-status.snapshot"`, UUIDv4 `event_id`. Compose service `tfl-line-status-producer` (profile `ingest`) + `redpanda-init` (profile `init`) + `src/ingestion/Dockerfile`. 16 unit tests + 1 integration smoke. |
| 3 | TM-B3 | `line-status` consumer | B-ingestion | ✅ 2026-04-28 | Async `LineStatusConsumer.run_forever` reads `line-status` topic with `group_id="tfl-monitor-line-status-writer"`, `auto_offset_reset="earliest"`, `enable_auto_commit=False`. `KafkaEventConsumer` wrapper mirrors TM-B2 producer. `RawLineStatusWriter` over single async psycopg connection with `INSERT … ON CONFLICT (event_id) DO NOTHING` for idempotency, reconnect-on-`OperationalError`. Failure isolation: poison pill = skip+commit; transient DB = reconnect+replay; unknown = log+replay. Lag traced on every `kafka.consume` span, refreshed every 50 msgs / 30 s. Compose service `tfl-line-status-consumer` (profile `ingest`) + `consume-line-status` task/Makefile. Observability helper `src/ingestion/observability.py` extracted (producer migrated). 21 unit tests + 1 integration smoke. |
| 3 | TM-C2 | dbt staging + first mart | C-dbt | ✅ 2026-04-28 | `stg_line_status` (incremental `merge` on `event_id`, JSONB unnested into typed columns, defensive `row_number()` dedup, `on_schema_change=fail`); `mart_tube_reliability_daily` at grain `(line_id, calendar_date UTC, status_severity)` with `snapshot_count`, `first/last_observed_at`, `minutes_observed_estimate` (1 snapshot ≈ 30 s, anchored on `LINE_STATUS_POLL_PERIOD_SECONDS`). Generic + 2 singular tests under `dbt/tests/`. `model-paths` extended to `["models", "sources"]` so dbt resolves the source YAML. No `dbt_utils` dependency. |
| 3 | TM-D2 | Wire endpoints to Postgres | D-api-agent | ✅ 2026-04-28 | `/status/live` queries `raw.line_status` (15-min freshness window, JSONB casts); `/status/history` queries `analytics.stg_line_status` with hard 30-day cap + `LIMIT 10000`; `/reliability/{line_id}` runs aggregate + histogram against `analytics.mart_tube_reliability_daily`, 404 on empty window, severities with zero count omitted, `reliability_percent` rounded to 1 decimal. `psycopg[binary,pool]` extra; `AsyncConnectionPool` (min 1, max 4) opened in FastAPI lifespan, optional — handlers return RFC 7807 `503` when `DATABASE_URL` is unset. New `src/api/{db,schemas}.py`; new `tests/conftest.py::FakeAsyncPool`; 14 unit tests + 4 integration smokes (gated on `DATABASE_URL`). `tests/api/test_stubs.py` STUB_ROUTES drops the three TM-D2 rows; bidirectional OpenAPI drift checks remain green. |
| 3 | TM-E1a | Next.js scaffold + shadcn + Network Now (mocked) | E-frontend | ✅ 2026-04-28 | scaffold + mocked data; real wiring in E1b post-D2. Vitest+RTL added (6 tests), `make check` chains `pnpm --dir web test`. |
| 3 | TM-E1b | Network Now wired to real `/status/live` | E-frontend | ✅ 2026-04-28 | `lib/api/status-live.ts` now calls `apiFetch<LineStatus[]>("/api/v1/status/live")`; `app/page.tsx` drops the mock-data banner and renders empty/error fallbacks (`ApiError` is caught and surfaced via an `Alert role="alert"`). Vitest covers happy path, empty array, non-2xx (`ApiError`), and network failure for the fetcher; the page test mocks `getStatusLive` with success/empty/error cases. Mock fixture `web/lib/mocks/status-live.json` removed; depends on PR #38 (TM-D2) for live data, swap itself is contract-driven against `contracts/openapi.yaml`. |
| 4 | TM-B4 | arrivals + disruptions topics | B-ingestion | ✅ 2026-04-28 | `ArrivalsProducer` polls 5 NaPTAN hubs every 30s (partition key `station_id`, `event_type="arrivals.snapshot"`); `DisruptionsProducer` polls 4 modes every 300s (partition key `disruption_id`, `event_type="disruptions.snapshot"`). Consumer + writer generalised: `RawEventConsumer[E]` + `RawEventWriter(table)` with allow-listed table names; line-status migrated in same PR (Logfire namespace `ingestion.line_status.*` preserved via `log_namespace` override). `redpanda-init` creates all three topics; four new Compose services on profile `ingest`. 27 new unit tests (arrivals + disruptions producers, parametrised consumer × 3 topics, 3 namespace whitebox cases) + smoke parametrised across topics. |
| 4 | TM-C3 | Remaining marts + tests + exposures | C-dbt | ✅ 2026-04-28 | `stg_arrivals` + `stg_disruptions` (incremental `merge`, 5 min lookback, DESC dedup); `mart_tube_reliability_daily_summary` at `(line_id, calendar_date)` with `pct_good_service` + gaps-and-islands `longest_disruption_window_minutes`; `mart_disruptions_daily` at `(calendar_date, line_id)` with affected_routes JSONB fan-out + `distinct_categories` + `max_severity`; `mart_bus_metrics_daily` at `(calendar_date, line_id, station_id)` filtering bus lines via `source('tfl', 'lines')` (returns 0 rows until TM-A2 populates `ref.lines`). 5 singular tests under `dbt/tests/` (pct bound, three composite-grain uniqueness, disruption category enum). 5 dbt exposures wiring marts to the warehouse-backed API endpoints. Marts run empty until TM-B4 lands; contracts frozen. |
| 4 | TM-D3 | Remaining endpoints | D-api-agent | ✅ 2026-04-28 | `/disruptions/recent` queries `analytics.stg_disruptions` (snapshot grain — daily mart drops the description/summary/affected_stops/closure_text/created/last_update fields the contract requires) sorted `last_update DESC NULLS LAST, ingested_at DESC` with `LIMIT %(limit)s` (1–200, default 50); the optional `mode` filter resolves through an `EXISTS` subquery against `analytics.stg_line_status` so we avoid the empty `ref.lines` dependency. `/bus/{stop_id}/punctuality` reads `analytics.stg_arrivals` directly (the `mart_bus_metrics_daily` filter through `ref.lines` returns zero rows on a fresh DB and the mart lacks `station_name` denormalisation); the on-time / early / late percentages are a documented PROXY composed via `FILTER` clauses on `time_to_station_seconds` (≤ 300 s = on-time, > 300 s = early, < 0 s = late, anchored to TfL's published 5-minute bus performance KPI). Window hard-coded to 7 days to match the OpenAPI example; the contract declares no `window` query param. Both endpoints reuse the TM-D2 lifespan / `AsyncConnectionPool` / `_problem` plumbing — pool missing → 503 RFC 7807; bus 404 RFC 7807 when `sample_size = 0` or `station_name` is missing with detail `"No punctuality data for stop {stop_id}"`. `tests/api/test_stubs.py::STUB_ROUTES` drops the two TM-D3 rows; bidirectional OpenAPI drift checks remain green. New: `DisruptionResponse`, `BusPunctualityResponse`, `DisruptionCategory` literal in `src/api/schemas.py`; `DISRUPTIONS_SQL`, `BUS_PUNCTUALITY_SQL`, `BUS_STOP_NAME_SQL`, `fetch_recent_disruptions`, `fetch_bus_punctuality` in `src/api/db.py`. 16 unit tests + 3 integration smokes (gated on `DATABASE_URL`). |
| 4 | TM-E2 | Disruption Log view | E-frontend | ✅ 2026-04-28 | `web/lib/api/disruptions-recent.ts` calls `apiFetch<Disruption[]>("/api/v1/disruptions/recent")` with `cache: "no-store"`. `web/app/disruptions/page.tsx` rewritten as an async server component mirroring TM-E1b: empty-state alert, error fallback that surfaces `ApiError.detail` (not the generic message prefix), per-disruption Card with summary + category badge (severe/minor/info/neutral tones for `RealTime`+`Incident` / `PlannedWork` / `Information` / `Undefined`), `affected_routes` rendered as a `<ul aria-label>` list of outline badges, `closure_text` surfaced in a destructive Alert when non-empty. Mock fixture `web/lib/mocks/disruptions-recent.json` deleted. Vitest covers fetcher (5 cases — URL/headers, schema invariants, empty array, `ApiError`, network failure) + page (9 cases — list, category-tone mapping, closure callout shown/hidden, affected-routes list, empty state, error fallback, `ApiError.detail` surfacing, non-`ApiError` rejection fallback). TM-25 disclaimer not surfaced here — this view does not consume bus punctuality data; the disclaimer applies when a Bus card is added. |
| 5 | TM-A2 | Airflow DAGs | A-infra | ✅ 2026-04-28 | Two DAGs under `airflow/dags/`: `dbt_nightly` (`0 1 * * *`) runs `dbt build` against the `dev` profile via `BashOperator` + `uv run`; `rag_ingest_weekly` (`0 3 * * 1`) re-runs `python -m rag.ingest` (idempotent on HTTP 304). Shared `default_args = {owner: tfl-monitor, retries: 2, retry_delay: 5min}`, `catchup=False`, `max_active_runs=1`. Compose mounts the project root at `/opt/tfl-monitor` with `working_dir` set on the three Airflow services so `uv run` resolves `pyproject.toml`; Airflow image installs `uv` via `airflow/requirements.txt`. Hermetic DAG-parse tests under `tests/airflow/` (4 tests) gated by `@pytest.mark.airflow` (excluded from default `addopts`); explicit run via `make airflow-test` or `uv run pytest -m airflow tests/airflow -v`. `static_ingest` DAG deferred until a `ref.lines`/`ref.stations` loader exists. |
| 5 | TM-D4 | RAG ingestion: Docling → Pinecone | D-api-agent | ✅ 2026-04-28 | `src/rag/` package: `sources.py` resolves the direct-PDF URL on each landing page (3 TfL strategy docs) via per-source allowlist regex; `fetch.py` does conditional GET (`If-None-Match` + `If-Modified-Since`) into `data/strategy_docs/`; `parse.py` wraps Docling 2.x `DocumentConverter` + `HybridChunker` (library defaults; the Docling 2.x constructor derives chunk size from its tokenizer); `embed.py` async-batches OpenAI `text-embedding-3-small` (100/req, retry on 429); `upsert.py` Pinecone serverless `tfl-strategy-docs` (1536 dim, cosine, AWS us-east-1), one namespace per `doc_id`, stable id = `sha256("{resolved_url}::{chunk_index}")`, delete-namespace-on-rollover idempotency; `ingest.py` orchestrator with `--refresh-urls`, `--force-refetch`, `--dry-run` flags, fail-loud on missing `PINECONE_API_KEY`/`OPENAI_API_KEY`. 28 unit tests with fakes for httpx/Docling/OpenAI/Pinecone. |
| 6 | TM-D5 | LangGraph agent (SQL + RAG + Pydantic AI) | D-api-agent | ✅ 2026-04-29 | New `src/api/agent/` package wiring `create_react_agent` (Sonnet `claude-3-5-sonnet-latest`, `temperature=0`) over five typed LangChain tools — four wrap the TM-D2/D3 fetchers in-process (`query_tube_status`, `query_line_reliability` with a Pydantic AI Haiku `LineId` normaliser, `query_recent_disruptions`, `query_bus_punctuality` carrying the proxy caveat in its docstring), the fifth is a LlamaIndex `PineconeVectorStore` retriever fanning out across the three TM-D4 namespaces (`tfl_business_plan`, `mts_2018`, `tfl_annual_report`) with optional `doc_id` targeting and `-1` page-sentinel mapping. `compile_agent` returns `None` when any of `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `PINECONE_API_KEY` is missing, so `/chat/stream` 503s while `/chat/{thread_id}/history` keeps working off `DATABASE_URL` alone. Lifespan compiles the graph once and caches it on `app.state.agent`; SSE handler streams via `agent.astream(stream_mode=["messages","updates"])` projected to `{type: token\|tool\|end, content}` frames by `streaming.project`, persists the user turn before the first frame and the assistant turn (concatenated tokens only) on stream end. New `analytics.chat_messages` table (DDL in `contracts/sql/004_chat.sql`) declared as a documented cross-track touch under ADR 002 §Consequences. `InMemorySaver` checkpointer; `pydantic-ai>=1.75` (was `>=0.0.14`) for anthropic 0.96 compat; two new deps (`langchain-anthropic`, `llama-index-vector-stores-pinecone`); zero new env vars. 36 unit tests (six SSE route, three history route, four history unit, four agent-extraction, eight agent-tools, six agent-RAG, five agent-graph) + three integration smokes (history round-trip + chat-stream + history-after-stream gated on the four-key combo). `tests/api/test_stubs.py::STUB_ROUTES` empties (last 501s removed); bidirectional OpenAPI drift checks remain green. |
| 6 | TM-E3 | Chat view with SSE | E-frontend | ✅ 2026-04-29 | `web/lib/sse-parser.ts` is a stateful 60-line parser that buffers `Uint8Array` chunks via a streaming `TextDecoder`, splits on `\n\n` boundaries, drops `:`-prefixed comments (sse-starlette `: ping`), and JSON-parses each `data:` line into a discriminated `{type: "token"\|"tool"\|"end", content: string}` `Frame`; malformed JSON / unknown types are silently dropped instead of throwing so a single corrupt frame never kills the stream. `web/lib/api/chat-stream.ts` exposes an `async function* streamChat(body, signal?)` that POSTs `ChatRequest` to `${NEXT_PUBLIC_API_URL ?? "http://localhost:8000"}/api/v1/chat/stream` with `Accept: text/event-stream`, raises `ApiError(status, detail)` (RFC 7807 `detail` / `title` extracted before `statusText` fallback) on non-2xx, and yields parsed `Frame`s from `response.body.getReader()` with `releaseLock()` in `finally` so generator early-termination via `AbortSignal` cancels cleanly. `web/components/chat-view.tsx` is a `"use client"` `Card` with: a per-mount `crypto.randomUUID()` thread id kept in `useRef`; a `messages` state of `{id, role, content, errored?}` (stable id satisfies Biome `noArrayIndexKey`); a `<textarea rows={3} maxLength={4000}>` with `Send` disabled while busy or input empty; an inline `Skeleton` inside the empty trailing assistant bubble until the first `token`; an ephemeral `Using tool: <name>…` status line cleared as soon as the next `token` arrives; a top-of-card `Alert` ("Agent unavailable") for `ApiError` / network failures (and the ghost user/assistant pair is rolled back via `prev.slice(0, -2)`); and a mid-stream `end:"error"` frame that flips `data-errored="true"` on the in-flight assistant `<li>` instead of dropping the bubble. `web/app/ask/page.tsx` is rewritten from the TM-E1a stub to render `<ChatView />` inside the standard `<main>` shell. `web/app/page.tsx` (Network Now landing) gains a single `Ask the agent →` link in its header — no shared nav component yet. Vitest covers all three layers: 6 parser cases (single frame, multi-frame chunk, partial chunk buffering, ping-comment skipping, unknown-type drop, malformed-JSON survival), 5 fetcher cases (happy-path frame stream + URL/headers/body assertion, 503 → `ApiError`, RFC 7807 `detail` surfacing, network `TypeError` propagation, multi-chunk reader buffering), and 5 `ChatView` smoke cases (empty shell, streaming tokens into the bubble, tool-status lifecycle clearing on first token, 503 alert + conversation rollback scoped via `findByRole("alert")` to disambiguate title/description, mid-stream `end:error` flagging via `data-errored`). Streaming `Response`s in tests are constructed from `ReadableStream<Uint8Array>` + `vi.stubGlobal("fetch", mock)` — no new deps (`@testing-library/user-event` deliberately skipped, `fireEvent` carries the surface). All 54 web tests + Biome + `tsc --noEmit` (via `next build`) green; `make check` end-to-end clean. Out of scope per plan §"What we're NOT doing": history endpoint, localStorage, shared nav, markdown rendering, AbortController UX, Playwright E2E, reconnect-on-disconnect. |
| 7 | TM-E5 | Web frontend dashboard + chat one-pager (collapse 4 routes) | E-frontend | ✅ 2026-05-12 | One-pager `/` orchestrates two `useAutoRefresh` polls (live status @ 30 s, disruptions @ 5 min, paused on `visibilitychange`) and fans the data into four cards plus `<ChatPanel>`: `<NetworkStatusCard>` with mode tabs (Tube / Elizabeth / Overground / DLR / Tram·River·Cable / Bus) persisted in `localStorage` (`tfl-monitor:last-mode`), `<HappeningNowCard>` (RealTime + Incident, sort by `last_update` DESC), `<ComingUpCard>` (PlannedWork, sort by `created` ASC, cap 5), `<NetworkPulseTile>` (pure derivation from `LineStatus[]` → severe/degraded/good/other tally with TfL-correct semantics — only severity 10 counts as good; severity 18 Part Closure is degraded). `<ChatPanel>` sticks as a 1/3 aside on `lg+` and falls back to a vaul Drawer triggered by a FAB on `<lg`; `<AboutSheet>` Radix Sheet hosts the project blurb in the header. Legacy `/disruptions`, `/reliability`, `/ask` deleted; `next.config.ts` issues 308 redirects to `/` so external links survive. Three sub-PRs ship the work — PR #51 (hook + label tokens + shadcn `sheet`/`drawer` primitives), PR #53 (six dashboard surfaces with 19 RTL cases + jsdom storage / `matchMedia` stubs for Node 25), PR #54 (`app/page.tsx` rewrite + integration test + redirects + legacy-route deletion). Lucide `Github` icon was deprecated upstream; the header link falls back to `ExternalLink`. `pnpm --dir web build` reports a single `/` route; vitest 68 / 68 green; `make check` end-to-end clean. Out of scope per plan §"What we're NOT doing": TfL brand colours, dark-mode polish, SWR/React Query, OG meta, Playwright E2E. |
| 7 | TM-A3 | Supabase provisioning | A-infra | ⬜ | |
| 7 | TM-A4 | Redpanda Cloud | A-infra | ⬜ | |
| 7 | TM-A5 | AWS deploy (EC2 t4g.small spot + Bedrock + free tiers) | A-infra | ⬜ | Plan committed PR #49 |
| 7 | TM-E4 | Vercel deploy | E-frontend | ⬜ | |
| 7 | TM-F1 | README polish + demo video + live URL | F-polish | ⬜ | |

## TM-C1 notes

- Every functional acceptance criterion declared in `SETUP.md` §7.3 for
  TM-C1 was already satisfied by the TM-000 scaffold (dbt project,
  profiles, sources mirror, CI diff gate, `dbt-parse` task). This WP
  closes as a zero-code PR after verifying the CI diff + parse pass;
  see `.claude/specs/TM-C1-{research,plan}.md`.

## TM-000 notes

Mid-execution deviations, captured as ADRs or inline footnotes:

- `[tool.uv.scripts]` in the original spec is not a real uv feature;
  replaced by `taskipy` in `[tool.taskipy.tasks]` with a slim Makefile
  for system orchestration.
- Next.js generator produced v16.2.4 (spec locked at 15); kept v16
  because current `create-next-app` defaults to it and v16 is
  backward-compatible for the app-router scaffold used here. Tech-stack
  table in ARCHITECTURE.md and `CLAUDE.md` tech-stack list updated to
  match. A formal ADR will be added if issues surface in TM-E1+.
- shadcn CLI flow changed: `--base=radix --preset=nova` matches the
  legacy "Default / Slate / CSS variables=Yes" intent and avoids
  interactive prompts.
- Contracts split into two tiers: tier-1 `tfl_api` for raw TfL
  responses (camelCase, optional-heavy) and tier-2 Kafka events
  (snake_case, frozen, per spec §5.1). See ADR 002.
